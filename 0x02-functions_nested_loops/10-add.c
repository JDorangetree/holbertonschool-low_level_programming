#include "holberton.h"
/**
 * add -  adds two integers.
 *@x: first int to add
 *@y: secod int to add
 * Description: This prints absolute value
 * Return: return the result
 */
int add(int x, int y)
{
	int suma;

	suma = x + y;
	return (suma);
}
