#include "holberton.h"
/**
 * swap_int -  swap two integers.
 *@a: first int to swap
 *@b: secod int to swap
 * Description: Swap the values of two integers
 * Return: return the result
 */
int _strlen(char *s)
{
	int i;

	i = 0;
	while(s[i] != '\0')
	{
		i++;
	}
	return(i);
}
